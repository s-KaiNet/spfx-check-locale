/**
 * Options object
 */
export interface Options {
  /**
   * Required absolute path to your SPFx solution
   */
  projectPath: string;
  /**
   * Whether to print errors in console
   */
  printErrors?: boolean;
}